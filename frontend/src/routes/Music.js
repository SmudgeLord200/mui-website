import * as React from 'react';
import { Container, Box, Typography } from '@mui/material';
// import { makeStyles } from '@mui/material';

// const theme = makeStyles(

// );

const Music = () => {

    return (
        <Typography>Msuic</Typography>
    )
}

export default Music;